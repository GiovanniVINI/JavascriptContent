// Condicional ternária 

// Exemplo 1:

var isMember = false
var shipping = isMember ? 2 : 10

console.log(`Frete: ${shipping} reais`)

// Exemplo 2:

var age = 18
var isAdult = age ? 'Adulto' : 'Não é adulto'

console.log(isAdult)