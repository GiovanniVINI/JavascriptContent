// Retorno padrão de uma function 

// ENTRADA -> PROCESSAMENTO -> SAIDA

// Exemplo 1

function fullName(name, surname) {
    return name + ' ' + surname;
}

let full = fullName('Bonieky', 'Lacerda')
console.log('Nome completo: ' + full)

// Exemplo 2

function number(x, y) {
    return x + y
}

console.log(number(5, 6))